#!/bin/bash
mkdir test #mkdir is a command that creates a directory
touch test/test.txt #touch is a command that creates a file
echo test
ls test #ls is a command that lists the contents of a directory
sleep 2 #sleep is a command that suspends the execution of the script for a given number of seconds
rm test/test.txt #rm is a command that removes a file
rmdir test #rmdir is a command that removes a directory
